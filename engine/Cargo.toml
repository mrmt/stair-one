[workspace]
members = ["core", "ffi", "render"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"

# 音のテストは数秒分をレンダーするので、dev でも最適化する
[profile.dev]
opt-level = 2

[profile.release]
opt-level = 3
codegen-units = 1
# wasm は panic 時に止まればよい (文字列やファイルパスを埋め込まない)
panic = "abort"
